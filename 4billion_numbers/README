
Question:How would you sort 4 billion 32-bit unique integers in 512MB of RAM?
Answer: The problem is solved using bits of character array. In Character array each bit represnts a number.
        When a number is received correponding bit is set. once all the inputs are received. The numbers are printed
        if the corresponding bits are set.
